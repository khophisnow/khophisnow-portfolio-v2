import Image from "next/image";
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, ArrowUpRight, Database, KeyRound, Network, ShieldCheck, Terminal } from "lucide-react";
import { cases } from "@/lib/portfolio-data";

export function generateStaticParams() { return cases.map((item) => ({ slug: item.slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const study = cases.find((item) => item.slug === slug); return study ? { title: `${study.name} Case Study | KhophiSnow`, description: study.brief } : {}; }

export default async function CaseStudyPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params;
  const study = cases.find((item) => item.slug === slug);
  if (!study) notFound();
  return <main className="scanline min-h-screen bg-ink text-white"><div className="cyber-grid pointer-events-none absolute inset-x-0 top-0 h-[560px]" /><header className="relative border-b border-white/10 bg-ink/85 backdrop-blur-xl"><nav className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 lg:px-8"><Link href="/#case-files" className="inline-flex items-center gap-2 text-sm font-bold text-mint hover:text-white"><ArrowLeft size={16} />Back home</Link><a href="/cv/somuah-julius-software-developer-cv-2026.pdf" className="border border-mint/40 px-4 py-2 text-sm font-bold text-mint hover:bg-mint hover:text-ink">Download CV</a></nav></header><section className="relative mx-auto max-w-7xl px-5 py-16 lg:px-8"><p className="font-mono text-sm text-amber">{study.code} / {study.year}</p><h1 className="mt-4 max-w-4xl text-5xl font-black leading-tight md:text-7xl">{study.name}</h1><p className="mt-4 text-lg font-semibold text-cyan">{study.role}</p><p className="mt-6 max-w-3xl text-lg leading-8 text-white/68">{study.brief}</p><div className="mt-8 flex flex-wrap gap-3">{study.links.map((link) => <a key={link.href} href={link.href} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 bg-mint px-4 py-3 font-bold text-ink hover:bg-white">{link.label}<ArrowUpRight size={16} /></a>)}</div></section><section className="border-y border-white/10 bg-white/[0.025]"><div className="mx-auto grid max-w-7xl gap-3 px-5 py-10 sm:grid-cols-2 lg:grid-cols-4 lg:px-8">{study.impact.map((item) => <div key={item} className="border border-white/10 bg-panel/70 p-5 font-mono text-lg font-black text-mint">{item}</div>)}</div></section><section className="mx-auto grid max-w-7xl gap-10 px-5 py-20 lg:grid-cols-[0.92fr_1.08fr] lg:px-8"><div><SectionTitle eyebrow="Architecture" title="How the system is structured" /><div className="mt-8 space-y-3">{study.architecture.map((item, index) => <div key={item} className="flex gap-4 border border-white/10 bg-panel/70 p-4"><span className="flex size-8 shrink-0 items-center justify-center border border-mint/30 bg-mint/10 font-mono text-xs text-mint">{index + 1}</span><p className="text-sm leading-6 text-white/66">{item}</p></div>)}</div></div><ArchitectureDiagram modules={study.modules} /></section><section className="border-y border-white/10 bg-white/[0.025]"><div className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><SectionTitle eyebrow="Threat model" title="Risks, controls, and engineering lessons" /><div className="mt-10 grid gap-5 lg:grid-cols-3">{study.threatModel.map((item) => <article key={item.risk} className="border border-white/10 bg-panel/75 p-5"><p className="font-mono text-xs text-amber">risk</p><h3 className="mt-2 text-xl font-black text-white">{item.risk}</h3><p className="mt-5 text-sm leading-7 text-white/66"><span className="font-bold text-mint">Control:</span> {item.control}</p><p className="mt-4 text-xs leading-6 text-white/48">{item.lesson}</p></article>)}</div></div></section><section className="border-y border-white/10 bg-white/[0.025]"><div className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><SectionTitle eyebrow="Project screen" title="What the project looks like" /><figure className="mt-8 overflow-hidden border border-white/10 bg-panel/70 p-3"><Image src={study.slug === "edumanage" ? "/images/edumanage-demo.webp" : "/images/whats-up-ucc.webp"} alt={` screenshot`} width={1536} height={864} className="aspect-video w-full object-cover" /><figcaption className="mt-3 font-mono text-xs text-mint">Project evidence</figcaption></figure></div></section><section className="mx-auto grid max-w-7xl gap-10 px-5 py-20 lg:grid-cols-2 lg:px-8"><div><SectionTitle eyebrow="Request examples" title="Endpoint surface" /><div className="mt-8 border border-white/10 bg-black/40 p-5 font-mono text-xs leading-7 text-cyan">{study.mock.map((item) => <p key={item}>{item}</p>)}</div></div><div><SectionTitle eyebrow="Stack" title="Tools used" /><div className="mt-8 flex flex-wrap gap-2">{study.stack.map((item) => <span key={item} className="border border-white/10 bg-panel/70 px-3 py-2 font-mono text-xs text-white/70">{item}</span>)}</div></div></section></main>;
}
function SectionTitle({ eyebrow, title }: { eyebrow: string; title: string }) { return <div><p className="font-mono text-sm uppercase text-mint">{eyebrow}</p><h2 className="mt-3 max-w-3xl text-3xl font-black leading-tight text-white md:text-5xl">{title}</h2></div>; }
function ArchitectureDiagram({ modules }: { modules: readonly string[] }) { return <div className="border border-white/10 bg-panel/75 p-5"><div className="grid gap-3"><DiagramNode icon={<Terminal size={18} />} label="Client" detail="Frontend requests" /><DiagramNode icon={<ShieldCheck size={18} />} label="Auth Layer" detail="JWT, guards, sessions" /><DiagramNode icon={<Network size={18} />} label="API Modules" detail={`${modules.length} mapped modules`} /><DiagramNode icon={<Database size={18} />} label="Database" detail="Relational workflows" /></div><div className="mt-6 flex flex-wrap gap-2">{modules.map((module) => <span key={module} className="border border-white/10 bg-black/25 px-2.5 py-1.5 font-mono text-xs text-white/58">{module}</span>)}</div></div>; }
function DiagramNode({ icon, label, detail }: { icon: React.ReactNode; label: string; detail: string }) { return <div className="arch-node flex items-center gap-3 border border-white/10 bg-black/25 p-4"><span className="flex size-10 items-center justify-center border border-mint/30 bg-mint/10 text-mint">{icon}</span><span><span className="block font-bold text-white">{label}</span><span className="block text-xs text-white/50">{detail}</span></span><KeyRound className="ml-auto text-white/20" size={16} /></div>; }
