import Image from "next/image";
import { certificates } from "@/lib/portfolio-data";
import { SmartBackLink } from "@/components/SmartBackLink";

export const metadata = { title: "Certifications | KhophiSnow", description: "Training certificates, recognition, leadership proof, and technical achievement evidence for KhophiSnow." };

export default function CertificationsPage(){return <main className="scanline min-h-screen bg-ink text-white"><section className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><SmartBackLink fallback="/" label="Back" /><div className="mt-12 grid gap-8 lg:grid-cols-[0.42fr_0.58fr]"><div><p className="font-mono text-sm uppercase text-mint">Certificates</p><h1 className="mt-3 text-4xl font-black md:text-5xl">Certifications and recognition</h1><p className="mt-5 max-w-xl text-sm leading-7 text-white/62">A focused record of training, leadership, and achievement proof. Each certificate is shown fully with context beneath it.</p></div><div className="border border-white/10 bg-panel/70 p-5"><p className="font-mono text-xs uppercase text-cyan">Quick index</p><div className="mt-4 grid gap-2">{certificates.map((cert)=><a key={cert.id} href={`#${cert.id}`} className="border border-white/10 bg-black/25 px-3 py-2 text-sm text-white/68 hover:border-mint hover:text-mint">{cert.title}</a>)}</div></div></div><div className="mt-12 space-y-10">{certificates.map((cert)=><article id={cert.id} key={cert.id} className="scroll-mt-24 border border-white/10 bg-panel/70 p-4"><div className="flex justify-center bg-white/[0.03] p-3"><Image src={cert.image} alt={cert.title} width={1600} height={1131} className="max-h-[760px] w-full object-contain object-top"/></div><div className="mt-5 grid gap-5 border-t border-white/10 pt-5 md:grid-cols-[0.36fr_0.64fr]"><div><p className="text-2xl font-black">{cert.title}</p><p className="mt-2 text-sm font-semibold text-mint">{cert.issuer}</p></div><p className="text-sm leading-7 text-white/62">{cert.detail}</p></div></article>)}</div></section></main>}
