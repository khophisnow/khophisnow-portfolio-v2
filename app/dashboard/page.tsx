import { BarChart3, Edit3, ShieldCheck, Terminal, Users } from "lucide-react";
import { SourceBackLink } from "@/components/SourceBackLink";
import { achievements, apiEvidence, cases, systemStatus } from "@/lib/portfolio-data";
export const metadata = { title: "Control Room | KhophiSnow", description: "A read-only control room showing project, evidence, achievement, and portfolio system status." };
export default function DashboardPage() { return <main className="scanline min-h-screen bg-ink text-white"><section className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><SourceBackLink fallback={{ href: "/", label: "Back home" }} sources={{}} /><div className="mt-12 flex items-center gap-3 text-mint"><BarChart3 /><p className="font-mono text-sm">Control room</p></div><h1 className="mt-4 text-4xl font-black md:text-5xl">KhophiSnow control room</h1><p className="mt-5 max-w-2xl text-sm leading-7 text-white/62">This is a read-only view for now. A real content area can come next, so new projects, writeups, certificates, and photos can be added without editing the site by hand.</p><div className="mt-10 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{systemStatus.slice(0,4).map((item)=>{const Icon=item.icon; return <div key={item.label} className="border border-white/10 bg-panel/70 p-5"><Icon className="text-mint"/><p className="mt-4 font-mono text-xs text-white/45">{item.label}</p><p className="mt-1 text-2xl font-black">{item.value}</p></div>})}</div><div className="mt-10 grid gap-5 lg:grid-cols-3"><Panel icon={<Terminal/>} title="Projects" items={cases.map((c)=>`${c.name}: ${c.impact[0]}`)} /><Panel icon={<ShieldCheck/>} title="Evidence" items={apiEvidence.map((e)=>e.label)} /><Panel icon={<Users/>} title="Achievements" items={achievements.slice(0,5)} /></div><div className="mt-10 border border-cyan/25 bg-cyan/10 p-5"><div className="flex items-center gap-3 text-cyan"><Edit3/><h2 className="font-bold text-white">Future content area</h2></div><p className="mt-4 text-sm leading-7 text-white/65">For the next phase, this can become a private editor for adding writeups, projects, certificates, and gallery images from a browser.</p></div></section></main> }
function Panel({icon,title,items}:{icon:React.ReactNode;title:string;items:string[]}){return <div className="border border-white/10 bg-panel/70 p-5"><div className="flex items-center gap-3 text-mint">{icon}<h2 className="font-bold text-white">{title}</h2></div><div className="mt-5 space-y-2">{items.map((item)=><p key={item} className="border border-white/10 bg-black/25 p-3 text-sm text-white/65">{item}</p>)}</div></div>}
